package com.mycompany.myapp.domain.enumeration;

/**
 * The PassStatusType enumeration.
 */
public enum PassStatusType {
    REQUESTED,
    PRINTED,
    ACTIVATED,
    READY,
    BLOCKED,
    CANCEL,
    EXPIRED,
}
